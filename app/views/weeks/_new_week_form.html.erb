<%= form_for(@week) do |f| %>
  <% if @week.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@week.errors.count, "error") %> prohibited this week from being saved:</h2>

      <ul>
      <% @week.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :firstDay, :value => @date_newWeekFirstDay %>
  <div class="actions">
    <%= f.submit "New Week (#{@str_newWeekFirstDay})", :class => 'btn btn-primary' %>
  </div>
<% end %>
